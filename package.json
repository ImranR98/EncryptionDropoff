{
  "name": "encryptiondropoff",
  "version": "1.0.0",
  "description": "App to watch a folder for new files, then notify the user and allow them to remotely initiate the moving of those files into an encrypted container.",
  "main": "app.js",
  "scripts": {
    "start": "NODE=$(which node) && sudo $NODE app.js"
  },
  "author": "Imran Remtulla",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  }
}
